# My neovim configuration

To install clone this repo into your neovim config dir by using following command from anywhere

```bash
git clone https://github.com/b1tka/neovim-config ~/.config/nvim
```


